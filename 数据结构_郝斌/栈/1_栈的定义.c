#include <stdio.h>
#include <stdlib.h>
/*
栈 stack
内存可以分配静态内存和动态内存
静态内存为栈 stack
动态内存为堆 heap
栈是吃完就吐， 队列是出完就拉

栈的定义：
    一种可以实现“先进后出“的存储结构
    栈类似于箱子

栈的分类：
    静态栈
    动态栈

栈的算法：
    出栈
    压栈

应用
    函数调用
    中断
    表达式求值
    内存分配
    缓冲处理
    迷宫
*/

void f(int k)
{
    int m;

    double* q = (double*)malloc(200);
}

int main(void)
{
    int i = 10;
    int* p = (int*)malloc(200);
    
    return 0; 
}